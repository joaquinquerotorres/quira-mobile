# Producción (valores no sensibles). Vite cargará esto al hacer `npm run build`.
#
# Valores sensibles / específicos de entorno deben ir en:
# - `.env.production.local` (NO se commitea; ya está en .gitignore), o
# - variables del sistema de build (CI / Xcode / Android Studio).

# Debe coincidir con el prefijo /api del backend (mismas rutas que en desarrollo con proxy).
VITE_API_URL=https://api.quira.app/api

